# API Key 발급 가이드

> 거래소 API Key 발급 방법을 안내합니다.

---

## 🔑 API Key 발급 (공통 절차)

모든 거래소의 API Key 발급 절차는 동일합니다.

```
① PC 웹에서 거래소 로그인
   → 모바일에서는 발급 불가
② 마이페이지 → API 관리 메뉴 이동
   → 거래소별 메뉴 위치는 아래 표 참고
③ 권한 설정 (조회 ✅ 주문 ✅)
   → ⚠ 출금 권한 반드시 OFF
④ IP 등록: 43.203.100.239
   → ⚠ 반드시 등록 (미등록 시 거래 불가)
⑤ API Key + Secret Key 복사/저장
   → ⚠ Secret Key는 이후 재확인 불가!
```

---

## 🏦 거래소별 API 메뉴 경로

| 거래소 | API 메뉴 위치 |
|--------|--------------|
| 빗썸 | 우측 상단 사람 아이콘 → 설정 → API 관리 |
| 업비트 | 마이페이지 → Open API 관리 |
| 코인원 | 우측 상단 C 아이콘 → 연동/API → 통합 API 관리 |
| 코빗 | 우측 상단 메뉴 → 설정 → API |
| 고팍스 | 계정관리(마이페이지) → 최하단 스크롤 → API 키 |

---

## ⚠ 거래소별 특이사항

- **빗썸** — API Key 생성 후 [활성화] 클릭 + 이메일 승인 링크 필요
- **업비트** — 2FA(2채널 인증) 필수 / API Key 유효기간 **1년**
- **고팍스** — API Key 유효기간 **1년** (만료 시 재발급)

---

## 🚨 공통 주의사항

> ⚠ 출금 권한은 반드시 OFF — API Key 유출 시 자산 보호

> ⚠ IP 등록: 43.203.100.239 — 미등록 시 거래 실행 불가

- **Secret Key는 발급 시 한 번만 표시** — 즉시 안전한 곳에 보관
- **PC 웹에서만 발급 가능** — 모바일 앱 불가
- API Key는 MyCoinBot에서 **AES 암호화하여 안전하게 저장**됩니다

---

*문의사항은 관리자에게 연락해주세요.*
